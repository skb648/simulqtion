name: Reality Compiler CI
on:
  push:
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with: {python-version: '3.13'}
      - run: python -m pip install --upgrade pip
      - run: pip install -r backend/requirements.txt
      - run: PYTHONPATH=. pytest -q

  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v5
        with: {distribution: temurin, java-version: '17'}
      - uses: gradle/actions/setup-gradle@v4
        with: {gradle-version: '9.5.0'}
      - run: gradle -p android/RealityCompiler testDebugUnitTest assembleDebug
      - name: Publish debug APK
        uses: actions/upload-artifact@v4
        with:
          name: reality-compiler-debug-apk
          path: android/RealityCompiler/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
